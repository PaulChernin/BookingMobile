<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paulchernin.booking">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Booking"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.MyBookingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MyBookingsListActivity"
            android:exported="false" />
        <activity
            android:name=".activities.NewBookingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.FiltersActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RoomsListActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>